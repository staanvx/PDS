services:
  scanner:
    build: .
    container_name: phishing-scanner
    env_file:
      - .env
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    command: ["uv", "run", "python", "app/main.py"]
